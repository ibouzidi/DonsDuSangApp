<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    x:Class="DonsDuSangApp.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://schemas.microsoft.com/dotnet/2021/maui/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:local="clr-namespace:DonsDuSangApp"
    xmlns:vm="clr-namespace:DonsDuSangApp.ViewModels"
    Padding="{OnPlatform iOS={StaticResource ApplePadding}, Default={StaticResource DefaultPadding}}"
    Title="Connexion"
    x:DataType="vm:LoginViewModel"
    mc:Ignorable="d">

    <ContentPage.Content>
        <StackLayout Padding="20" Spacing="15">

            <Label Text="Connexion Donneur" FontSize="24" FontAttributes="Bold" HorizontalOptions="Center" />

            <Entry Placeholder="Email" Text="{Binding Email}" Keyboard="Email" />
            <Entry Placeholder="Mot de passe" Text="{Binding Password}" IsPassword="True" />

            <Button Text="Se connecter" Command="{Binding ConnexionCommand}" Style="{StaticResource PrimaryAction}" />

        </StackLayout>
    </ContentPage.Content>
</ContentPage>
